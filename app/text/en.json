{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "game_title": "Hexim",
  "game_tiles": [
    {
      "declarations": ["input count", "local tiles = count: plural"],
      "match": {
        "tiles=one": "{count} tile",
        "tiles=other": "{count} tiles"
      }
    }
  ]
}
